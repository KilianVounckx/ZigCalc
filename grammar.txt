expression  : term ([PLUS | MINUS] term)*
            | EXIT

term        : factor ([ASTERISK | SLASH] factor)*

factor      : (MINUS)? factor
            | LEFT_PAREN expression RIGHT_PAREN
            | number
            | ANS